name: CI
on: [push, pull_request]

# TODO: cache ~/Library/Developer/Xcode/DerivedData or use actions/cache keyed on Package.resolved once AppCore package grows dependencies

defaults:
  run:
    # explicitly set to bash to enable pipefail
    shell: bash

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - name: Install Xcode Utilities
        run: brew install xcodegen xcbeautify
      - name: Generate project
        run: xcodegen generate
      - name: Build
        run: |
          xcodebuild build \
            -project Forge.xcodeproj \
            -scheme Forge \
            -destination 'platform=iOS Simulator,name=iPhone 17' \
            -skipMacroValidation \
            | xcbeautify

  test-core:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - name: Install Xcode Utilities
        run: brew install xcbeautify
      - name: Unit tests
        run: |
          cd Packages/ForgeCore
          xcodebuild test \
            -scheme ForgeCore \
            -destination 'platform=iOS Simulator,name=iPhone 17' \
            -resultBundlePath ../../ForgeCoreTests.xcresult \
            | xcbeautify
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: forge-core-test-results
          path: ForgeCoreTests.xcresult
          if-no-files-found: error

  test-ui:
    runs-on: macos-latest
    needs: build
    steps:
      - uses: actions/checkout@v6
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - name: Install Xcode Utilities
        run: brew install xcodegen xcbeautify
      - name: Generate project
        run: xcodegen generate
      - name: UI tests
        run: |
          xcodebuild test \
            -project Forge.xcodeproj \
            -scheme Forge \
            -destination 'platform=iOS Simulator,name=iPhone 17' \
            -only-testing:ForgeUITests \
            -resultBundlePath ForgeUITests.xcresult \
            | xcbeautify
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: forge-ui-test-results
          path: ForgeUITests.xcresult
          if-no-files-found: error
